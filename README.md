# Notes
- spinners 
  - combo boxes
    - use cases:
    1. the size of the pizza
    2. choosing the current order
- Toast
  - message for completion or action done

- AlertDialog 
  - is for when the user wants to delete an item
  - user adds to many toppings

# Make sure

- Remove ALL “hardcoded text” warnings by defining names for string literals in the values/string.xml. -1 point
  for each violation, with a maximum of 5 points off.
- You MUST use Toast, AlertDialog, ImageView, Spinner, and ListView
- You MUST use a RecyclerView for a list of pizzas combining Chicago and NYC style specialty pizzas and build-
  your-own pizzas or use a RecyclerView for a list of topping options;
- Each option in the RecyclerView must include an image associated with the option,